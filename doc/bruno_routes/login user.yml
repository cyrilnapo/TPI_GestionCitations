info:
  name: login user
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost/api/login
  headers:
    - name: Content-Type
      value: application/json
    - name: X-CSRF-TOKEN
      value: "{{csrfToken}}"
  body:
    type: json
    data: |-
      {
        "username": "cyril",
        "password": "password123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
