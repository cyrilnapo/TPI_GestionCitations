info:
  name: profile get me
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost/api/profile
  headers:
    - name: X-CSRF-TOKEN
      value: "{{csrfToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
